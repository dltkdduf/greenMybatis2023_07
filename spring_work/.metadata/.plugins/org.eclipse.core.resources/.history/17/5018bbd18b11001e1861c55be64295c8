package com.example.demo.control;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
public class Path_1Controller {

	@RequestMapping("/path/view1") //고정값이 우선순위가 높다.
	String pathView1(Model mm) {
		System.out.println("pathView1 실행");
		mm.addAttribute("title","view1");
		return "path/view1";
	}
	
	@RequestMapping("/path/{depth1}")
	String pathDepth1(Model mm, @PathVariable String depth1) {
		System.out.println("pathDepth1 실행:"+depth1);
		mm.addAttribute("title","depth1");
		return "path/view1";
	}
	
	
	@RequestMapping("/path/{depth1}/list")
	String pathList(Model mm, @PathVariable String depth1) {
		System.out.println("pathList 실행:"+depth1);
		mm.addAttribute("title","list");
		return "path/view1";
	}
}
